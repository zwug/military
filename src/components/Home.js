const React = require('react');

const Home = React.createClass({
  render() {
    return (
      <div className="container">
        <div className="home-img"/>
      </div>
    );
  }
});

module.exports = Home;
