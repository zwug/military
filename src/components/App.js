const React = require('react');
const Header = require('components/Header');

const App = React.createClass({
  render() {
    return (
      <div>
        <Header />
      </div>
    );
  }
});

module.exports = App;
